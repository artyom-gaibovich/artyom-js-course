
['', 'Filled string', '', ''].forEach(((string, index) => {
    if (!string) {
        console.log(`Элемент с индексом ${index} является пустым`);
    }
}))


const stringsArray = ['1', 'Filled string', '2', '3'];

for (let i = 0; i < stringsArray.length; i++) {
    console.log(stringsArray[i]);
}
