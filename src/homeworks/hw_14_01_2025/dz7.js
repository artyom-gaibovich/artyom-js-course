//7) Создайте массив с произвольными элементами. Выведите на экран количество элементов в этом массиве.

let someMass = [1, 2, 3, 4, "b", "b"]

let elementNum = 0

for (let i = 0; i < someMass.length; i++) {
    elementNum++
}
console.log(elementNum)