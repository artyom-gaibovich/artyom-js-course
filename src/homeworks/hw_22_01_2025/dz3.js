let massWithDiffTypesOfData = ["A", 100, false];

// Сложность с взаимодействии с ними так как к каждой нужен свой метод для взаимодействия, а другие не подходят
// Несовместимость если попытаться их сложить будет ошибка так как например строку с числом нельзя суммировать
