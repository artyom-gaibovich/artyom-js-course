/*- Даны два массива чисел: array1 = [1, 2, 3, 4] и array2 = [5, 6, 7, 8].
Напиши программу, которая:
     1. Объединит оба массива.
     2. Умножит каждый элемент на 2 с помощью map.
     3. Отфильтрует числа больше 10.
     4. Напечатает их на экран с помощью forEach.
 */
 ;
;

[...[1, 2, 3, 4], ...[5, 6, 7, 8]].map(num => num*2).filter(num => num > 10).forEach(num => console.log(num));


