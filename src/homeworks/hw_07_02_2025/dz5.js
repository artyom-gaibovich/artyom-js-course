/*Создай объект bankAccount с методами:
    - deposit(amount) — увеличивает баланс
- withdraw(amount) — уменьшает баланс (но не позволяет уйти в минус)
- getBalance() — возвращает текущий баланс*/

const bankAccount = {
    balance: 0,
}