# 📘 **Урок: JSON в JavaScript**

## **1. Что такое JSON?**

**JSON** — это **JavaScript Object Notation** (нотация объектов JavaScript).

* Формат обмена данными между фронтендом и бэкендом
* Читаем человеком
* Совместим со многими языками программирования
* Используется практически на всех веб-сайтах

**Особенности JSON:**

* Стартует с `{}` (объект) или `[]` (массив)
* Все **ключи и строки** должны быть в двойных кавычках `" "`
* Допустимые типы значений:

    * строки `"text"`
    * числа `5`
    * булевы значения `true`/`false`
    * массивы `["a", "b"]`
    * вложенные объекты `{ key: value }`
* **Трейлинг-комы запрещены** (после последнего элемента не ставим запятую)

---

## **2. Методы работы с JSON**

JSON в JavaScript предоставляет два основных метода:

| Метод              | Что делает                          | Пример                              |
| ------------------ | ----------------------------------- | ----------------------------------- |
| `JSON.parse()`     | Преобразует JSON-строку в объект JS | `const obj = JSON.parse('{"a":1}')` |
| `JSON.stringify()` | Преобразует объект JS в JSON-строку | `const str = JSON.stringify({a:1})` |

---

### **2.1 JSON.parse()**

Превращает строку в объект:

```js
const jsonString = '{"A":1}';
const obj = JSON.parse(jsonString);

console.log(obj); // { A: 1 }
console.log(obj.A); // 1
```

**Важно:**

* Если JSON **невалиден**, например `{"A":1,}` (трейлинг-кома), произойдет **ошибка**
* Ошибки можно обрабатывать через `try-catch` (изучается позже)

---

### **2.2 JSON.stringify()**

Превращает объект в строку:

```js
const obj = { A: 1 };
const str = JSON.stringify(obj);

console.log(str); // '{"A":1}'
console.log(typeof str); // "string"
```

**Применение:**

* Сохранение объектов в **Local Storage**
* Передача данных на сервер
* Логирование объектов в читаемом формате

---

## **3. Пример использования JSON с Local Storage**

```js
const user = { name: "Ann", age: 25 };

// Сохраняем объект в Local Storage
localStorage.setItem("user", JSON.stringify(user));

// Читаем объект из Local Storage
const storedUser = JSON.parse(localStorage.getItem("user"));

console.log(storedUser); // { name: "Ann", age: 25 }
console.log(storedUser.name); // "Ann"
```

**Почему это важно:**

Local Storage может хранить только **строки**, поэтому объекты нужно сериализовать через `JSON.stringify`.

---

## **4. Основные ошибки при работе с JSON**

1. Использование **невалидного JSON**:

```js
const badJson = '{"A":1,}';
JSON.parse(badJson); // ❌ Ошибка
```

2. Попытка положить объект в Local Storage без `stringify`:

```js
const obj = { name: "Ann" };
localStorage.setItem("user", obj); // сохранится как "[object Object]"
```

**Вывод:**
✅ Всегда используем `JSON.stringify` при сохранении объектов
✅ Всегда используем `JSON.parse` при чтении объектов

---

## **5. Мини-практика**

1. Создать объект с вашими данными:

```js
const task = { title: "Изучить JSON", done: false };
```

2. Превратить объект в строку и сохранить в Local Storage:

```js
localStorage.setItem("task", JSON.stringify(task));
```

3. Прочитать объект обратно и вывести в консоль:

```js
const savedTask = JSON.parse(localStorage.getItem("task"));
console.log(savedTask.title); // "Изучить JSON"
```
