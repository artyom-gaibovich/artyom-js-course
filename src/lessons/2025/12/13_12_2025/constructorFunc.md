# 📘 Урок Реализация ООП в JavaScript: Constructor Functions и `new`

## 🎯 Цель урока

Понять:

* как в JavaScript создаются объекты по шаблону,
* как работает оператор `new`,
* что такое **функция-конструктор**,
* как из одного blueprint создавать неограниченное число инстансов.

---

## 1. Зачем нам operator `new`

Представим задачу:
нам нужно создавать пользователей — много пользователей, с разными данными, но по одной схеме.

Для этого в JavaScript можно использовать **функцию-конструктор** и оператор `new`.

---

## 2. Функция-конструктор

По соглашению:

* функции-конструкторы пишутся **с большой буквы**,
* это не требование языка, а стандарт читаемости.

Пример:

```js
function User(email, password) {
  this.email = email;
  this.password = password;
}
```

Эта функция — **blueprint** (шаблон), а не конкретный пользователь.

---

## 3. Создание объекта с помощью `new`

Создадим пользователя:

```js
const user1 = new User("a@mail.ru", "123");
console.log(user1);
```

Результат:

* объект пользователя,
* с полями `email` и `password`,
* связанный с прототипом `User`.

---

## 4. Что на самом деле делает оператор `new`

Когда вы пишете:

```js
new User("a@mail.ru", "123");
```

JavaScript выполняет **4 шага**:

### 1️⃣ Создаётся пустой объект

```js
{}
```

### 2️⃣ Вызывается функция `User`

```js
User(email, password)
```

### 3️⃣ `this` внутри функции указывает на новый объект

```js
this === {}
```

И мы можем добавлять свойства:

```js
this.email = email;
this.password = password;
```

### 4️⃣ Объект связывается с `User.prototype`

и **автоматически возвращается**, даже если `return` не указан.

---

## 5. Роль `this` в конструкторе

Без `new`:

```js
User("a@mail.ru", "123"); // this === undefined (в strict mode)
```

С `new`:

```js
const user = new User("a@mail.ru", "123");
// this === user
```

👉 Именно `new` подставляет `this` и делает магию возможной.

---

## 6. Создание нескольких инстансов

```js
const user1 = new User("a@mail.ru", "123");
const user2 = new User("b@gmail.com", "456");

console.log(user1);
console.log(user2);
```

Результат:

* два разных объекта,
* созданных по одному шаблону,
* с разными данными.

---

## 7. Проверка инстанса: `instanceof`

JavaScript позволяет проверить, из какого конструктора был создан объект:

```js
console.log(user1 instanceof User); // true
```

Это часто используется:

* для проверки типов,
* для фильтрации объектов,
* для валидации данных.

---

## 8. Имена параметров и свойств

Имена аргументов и свойств могут **не совпадать**:

```js
function User(mail, pass) {
  this.email = mail;
  this.password = pass;
}
```

Это нормально.

Но на практике:

* обычно используют одинаковые имена,
* так код читается проще и понятнее.

---

## 9. Почему этот способ считается «устаревшим»

Функции-конструкторы:

* работают,
* широко использовались раньше,
* лежат в основе классов.

Но в современном JavaScript:

* чаще используют **ES6 classes**,
* они читаемее,
* проще для понимания,
* лучше поддерживают наследование.

⚠️ Тем не менее, **понимать constructor functions обязательно**,
потому что:

* классы работают на их основе,
* вы будете встречать такой код в старых проектах.

---

## 10. Итоги урока

Итог:

✔ что такое функция-конструктор
✔ как работает оператор `new`
✔ какие шаги он выполняет
✔ зачем нужен `this`
✔ как создавать инстансы
✔ как проверять их через `instanceof`

---
