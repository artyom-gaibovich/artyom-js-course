# 📘 Урок Методы и `prototype` в JavaScript

## 🎯 Цель урока

Понять:

* почему нельзя объявлять методы прямо в конструкторе,
* что такое `prototype`,
* как правильно добавлять методы объектам,
* как работает прототипная цепочка.

---

## 1. Проблема методов внутри конструктора

Начнём с наивного варианта:

```js
function User(email, password) {
  this.email = email;
  this.password = password;

  this.sayHello = function () {
    console.log(`Hello, ${this.email}`);
  };
}
```

Работает? ✅
Но есть **проблема**.

---

## 2. В чём проблема такого подхода

```js
const user1 = new User("a@mail.ru", "123");
const user2 = new User("b@mail.ru", "456");
```

❌ Для **каждого пользователя**:

* создаётся **новая функция `sayHello`**,
* она занимает новую область памяти.

Это:

* неэффективно,
* плохо масштабируется,
* противоречит идее переиспользования.

---

## 3. Решение: `prototype`

В JavaScript **методы должны жить в прототипе**, а не в каждом объекте.

```js
function User(email, password) {
  this.email = email;
  this.password = password;
}

User.prototype.sayHello = function () {
  console.log(`Hello, ${this.email}`);
};
```

Теперь:

```js
const user1 = new User("a@mail.ru", "123");
const user2 = new User("b@mail.ru", "456");

user1.sayHello();
user2.sayHello();
```

✅ Один метод
✅ Общее использование
✅ Экономия памяти

---

## 4. Как это работает под капотом

Когда вы пишете:

```js
user1.sayHello();
```

JavaScript ищет метод:

1️⃣ в самом объекте `user1`
2️⃣ если не найден — в `User.prototype`
3️⃣ дальше — выше по цепочке (`Object.prototype`)

Это называется **прототипная цепочка**.

---

## 5. Визуализация прототипной цепочки

```
user1
  ↓ [[Prototype]]
User.prototype
  ↓ [[Prototype]]
Object.prototype
  ↓
null
```

---

## 6. Проверка прототипа

```js
console.log(user1.__proto__ === User.prototype); // true
```

(⚠️ `__proto__` используется только для обучения, в реальном коде — не рекомендуется)

---

## 7. Добавление нескольких методов

```js
User.prototype.checkPassword = function (password) {
  return this.password === password;
};

User.prototype.changePassword = function (newPassword) {
  this.password = newPassword;
};
```

Использование:

```js
user1.checkPassword("123"); // true
user1.changePassword("qwerty");
```

---

## 8. Свойства vs методы

| Где хранить | Что хранить           |
| ----------- | --------------------- |
| `this`      | индивидуальные данные |
| `prototype` | общее поведение       |

Пример:

```js
// данные
this.email = email;

// поведение
User.prototype.sayHello = ...
```

---

## 9. Частая ошибка

❌ Перезаписывать prototype объектом:

```js
User.prototype = {
  sayHello() {},
  checkPassword() {}
};
```

Так вы:

* потеряете `constructor`,
* сломаете цепочку наследования.

✔ Правильно:

```js
User.prototype.sayHello = function () {};
```

---

## 10. Итоги урока

Вы узнали:

✔ почему методы нельзя класть в конструктор
✔ что такое `prototype`
✔ как работают методы через прототип
✔ что такое прототипная цепочка
✔ как JavaScript ищет свойства

