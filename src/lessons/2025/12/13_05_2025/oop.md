# 📘 Теория: Что такое объектно-ориентированное программирование (ООП)

## 🎯 Цель урока

Понять, что такое объектно-ориентированное программирование, зачем оно нужно, из каких основных идей состоит и почему оно важно для разработчика на JavaScript.

---

## 1. Что такое ООП

**Объектно-ориентированное программирование (ООП)** — это одна из парадигм программирования, то есть подходов к построению программ.

Суть ООП заключается в том, что программа моделируется как **набор взаимодействующих объектов**, похожих на объекты из реального мира.

Примеры объектов:

* пользователь,
* товар,
* корзина,
* телефон,
* ноутбук,
* заказ.

Каждый объект:

* хранит **данные** (свойства),
* умеет выполнять **действия** (методы),
* взаимодействует с другими объектами.

---

## 2. Почему ООП похоже на реальную жизнь

В реальной жизни мы думаем объектами:

* есть **телефон** → у него есть марка, модель, экран
* телефон умеет → включаться, выключаться, менять громкость

В ООП мы делаем то же самое:

* объединяем данные и поведение в одну сущность — **объект**,
* описываем, как объекты взаимодействуют между собой.

Например:

* пользователь кладёт товар в корзину,
* корзина считает итоговую стоимость,
* заказ создаётся на основе корзины.

---

## 3. Зачем вообще нужно ООП

На маленьких программах можно писать код линейно — «сверху вниз».
Но когда приложение становится большим, возникают проблемы:

* код сложно читать,
* много дублирования,
* трудно поддерживать и изменять,
* сложно понять, где что происходит.

**ООП было создано как решение проблемы структурирования больших приложений.**

Преимущества ООП:

* код становится более понятным,
* логика разделена по объектам,
* меньше дублирования,
* проще поддерживать и развивать проект,
* легче работать в команде.

---

## 4. ООП и JavaScript

JavaScript — **мультипарадигменный язык**:

* можно писать в функциональном стиле,
* можно писать в объектно-ориентированном стиле.

Важно:

* JavaScript **не всегда был классовым**,
* изначально ООП в JS строилось на **прототипах**,
* синтаксис `class` появился только в ES6.

Тем не менее:

* Angular активно использует ООП,
* NestJS на бэкенде построен на ООП,
* понимать ООП для JS-разработчика крайне важно.

---

## 5. Класс и объект (инстанс)

### Класс

**Класс** — это чертёж, описание структуры и поведения объекта.

Он определяет:

* какие свойства будут у объекта,
* какие методы он будет иметь.

### Объект (инстанс)

**Объект** — это конкретный экземпляр класса.

Пример:

* класс — «Айфон» (чертёж),
* объект — конкретный айфон у вас в руках.

По одному классу можно создать **сколько угодно объектов**.

---

## 6. Основные принципы ООП

Существует **4 ключевых принципа ООП**:

1. Абстракция
2. Инкапсуляция
3. Наследование
4. Полиморфизм

Разберём каждый из них на понятных примерах.

---

## 7. Абстракция

**Абстракция** — это выделение только того, что важно для текущей задачи, и игнорирование всего лишнего.

Пример:

* при работе с ноутбуком нам важно, что есть экран и клавиатура,
* нам не важно, как именно внутри работает процессор.

В программировании:

* мы описываем объект **не полностью**,
* а только те свойства и методы, которые нужны в конкретном контексте.

Один и тот же объект может быть абстрагирован по-разному:

* хлеб в магазине → цена, срок годности,
* хлеб на производстве → количество зерна, технология выпечки.

---

## 8. Инкапсуляция

**Инкапсуляция** — это сокрытие внутренней реализации объекта и предоставление наружу только нужного интерфейса.

Идея простая:

* есть **публичное** (доступно извне),
* есть **приватное** (скрыто внутри объекта).

Пример:

* пользователь может добавить товар в корзину,
* но не может напрямую изменить итоговую цену.

Зачем это нужно:

* защита данных,
* предотвращение ошибок,
* контроль логики внутри объекта.

---

## 9. Наследование

**Наследование** — это возможность одному классу перенимать свойства и методы другого класса.

Пример:

* есть общий класс «Ноутбук»,
* у него есть экран, клавиатура, тачпад,
* есть класс «MacBook», который наследуется от ноутбука.

Благодаря наследованию:

* не нужно переписывать одинаковую логику,
* код становится переиспользуемым,
* проще расширять функциональность.


---

## 10. Полиморфизм

**Полиморфизм** — это возможность работать с объектами через общий интерфейс, не зная их конкретной реализации.

Проще говоря:

* метод называется одинаково,
* но работает по-разному в разных объектах.

Пример:

* сервисный центр загружает ноутбук,
* не важно, MacBook это или Chromebook,
* главное — у ноутбука есть метод `boot`.

Как именно происходит загрузка — неважно для сервиса.

Полиморфизм позволяет:

* писать более гибкий код,
* легко заменять и расширять объекты,
* работать с разными реализациями одинаково.

---

## 11. Почему принципы ООП важны

* Они лежат в основе архитектуры приложений.
* На них базируются более сложные концепции (например, SOLID).
* Они помогают писать поддерживаемый и масштабируемый код.

Язык можно считать объектно-ориентированным, **только если он поддерживает эти принципы**.

---

## 📌 Итоги

В этом теоретическом уроке вы узнали:

* что такое ООП и зачем оно нужно,
* почему ООП помогает структурировать большие приложения,
* что такое классы и объекты,
* 4 основных принципа ООП:

    * абстракция,
    * инкапсуляция,
    * наследование,
    * полиморфизм,
* почему понимание ООП важно для JavaScript-разработчика.

