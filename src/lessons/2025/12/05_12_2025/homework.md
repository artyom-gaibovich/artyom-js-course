# 🎓 **ДОМАШНЕЕ ЗАДАНИЕ ПО ДАТАМ (JS) + setInterval, timers**

### **Тема: даты, timestamp, вычисления, сравнения, работа с setTimeout / setInterval**

---

# 🧩 **Часть 1. Работа с датами и timestamp**

### **Задача 1**

Создай дату: `1 февраля 2025 года`.
Выведи:

* год
* месяц
* день
* timestamp
* число миллисекунд с начала дня (через `getHours`, `getMinutes`, `getSeconds`, `getMilliseconds`)

---

### **Задача 2**

Сконвертируй дату в число:

```js
const d = new Date(2024, 11, 10);
```

Выведи:

1. `Number(d)`
2. `+d`
3. `d.getTime()`

Проверь — все ли значения одинаковые?

---

### **Задача 3**

Создай две даты с разницей в 10 секунд.
Выведи разницу *в миллисекундах* через вычитание:

```js
date2 - date1
```

---

# 🧮 **Часть 2. Вычисления разницы между датами**

### **Задача 4**

Напиши функцию:

```js
function getDaysBetween(date1, date2) { … }
```

Она должна возвращать количество дней между двумя датами.
Проверь функцию на примере:

* `2024-01-01`
* `2024-02-15`

Ожидаемый результат: `45`

---

### **Задача 5**

Создай функцию:

```js
function getWeeksBetween(date1, date2)
```

Которая возвращает количество недель (целое число).
Подсказка: дни → делим на 7.

---

### **Задача 6 (усложнённая)**

Сделай функцию:

```js
function isSameMonth(date1, date2)
```

Она возвращает `true`, если месяц и год совпадают.

---

# ⚖️ **Часть 3. Сравнение дат**

### **Задача 7**

Создай две одинаковые даты.
Проверь:

```js
a == b
a === b
a.getTime() === b.getTime()
```

Объясни, почему первое и второе дают `false`.

---

### **Задача 8**

Напиши функцию:

```js
function isEarlier(d1, d2)
```

Возвращает `true`, если d1 раньше d2.
Можно сравнить даты напрямую (`<`).

---

### **Задача 9**

Напиши функцию:

```js
function isSameDay(d1, d2)
```

Она должна проверять совпадают ли:

* день
* месяц
* год

---

# ⏱️ **Часть 4. Таймеры и интервалы**

### **Задача 10**

Создай интервал, который выводит время (`new Date()`) каждые 2 секунды.
Через 8 секунд останови интервал.

---

### **Задача 11**

Напиши функцию:

```js
function runNTimes(fn, count, delay)
```

Она должна выполнять `fn` *count* раз с задержкой `delay`.
Используй `setInterval` или `setTimeout` (рекурсивный timeout).

Пример:

```js
runNTimes(() => console.log("Hello"), 5, 1000);
```

---

# 🎉 **Часть 5. Мини-проект**

### **Проект A: Проверка дня рождения**

```js
function isBirthday(user)
```

User объект:

```js
{
  name: "Alice",
  birthday: "2000-02-01"
}
```

Если месяц + день совпадают — вернуть `true`.

---

### **Проект B: “Сколько дней до Нового Года”**

Сделай функцию:

```js
function daysUntilNY()
```

Она должна возвращать количество дней до ближайшего 1 января.

---

### **Проект C (усложнённый): Возраст**

Создай функцию:

```js
function getAge(birthday)
```

Она должна вернуть возраст человека (годы), учитывая:

* месяц рождения
* день рождения
* текущую дату

---

# ❓ **Часть 6. Вопросы на понимание**

1. Что такое timestamp?
2. Почему `new Date() == new Date()` → `false`?
3. Чем отличается `getDay()` от `getDate()`?
4. Почему setTimeout может сработать позже, чем указано?
5. Какие математические операции можно делать над датами?

