/**
 *
 * fetch(url)
 *   .then(...)
 *   .then(...)
 *   .catch(error => console.error(error));
 */


// Синтаксис

/**
 * try {
 *   // код, который может выбросить ошибку
 * } catch (error) {
 *   // обработка ошибки
 * }
 */